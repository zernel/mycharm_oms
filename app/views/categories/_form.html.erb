<%= simple_form_for(@category) do |f| %>
  <div class="form-inputs">
    <%= f.input :name, label: "商品种类名称" %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "提交表单" %>
  </div>
<% end %>
